<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.bebabeba">

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS" />
    <uses-permission android:name="android.permission.READ_PROFILE" />
    <uses-permission android:name="android.permission.READ_CONTACTS" />
    <uses-permission android:name="android.permission.CALL_PHONE" />

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality. 
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="com.google.android.providers.gsf.permission.READ_GSERVICES" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".DriverLoginActivity" />
        <activity android:name=".DriverRegisterActivity" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/. 
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="AIzaSyBCqZPMGk8R0My3nguZLNHnCoZjI4P3nsE" />

        <activity android:name=".DriverUserAreaActivity" />
        <activity android:name=".PassengerLoginActivity" />
        <activity android:name=".PassengerRegisterActivity" />
        <activity android:name=".PassengerUserAreaActivity" />
        <activity android:name=".PassengerMapActivity" />
        <activity android:name=".DriverMapActivity" />
        <activity android:name=".DisplayList" />
        <activity android:name=".SetCharges" />
        <activity android:name=".BookingsActivity" />
        <activity android:name=".DriverUpdateActivity" />
        <activity android:name=".ListViewActivity" />
        <activity android:name=".DriverUploadImage" />
        <activity android:name=".PassengerEditActivity" />
        <activity android:name=".PassengerWelcomeActivity" />
        <activity android:name=".PassengerBookingActivity" />
        <activity android:name=".PassengerViewBookings" />
        <activity android:name=".PassengerDisplayBooking" />
        <activity android:name=".DriverBookingDisplay" />
        <activity android:name=".DriverPaymentActivity" />
        <activity android:name=".PassengerPaymentActivity" />
        <activity android:name=".RateDriver"></activity>
    </application>

</manifest>